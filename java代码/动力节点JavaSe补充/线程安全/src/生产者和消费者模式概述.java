public class 生产者和消费者模式概述 {
    /**
     * 为了达到生产和需求平衡
     * Object类中的wait和notify方法
     *      所有对象都有
     *      wait
     *          让正在当前对象上活动的线程进入无期限的等待状态，直到被唤醒
     *          释放对象锁
     *
     *      notify或者notifyAll
     *          唤醒正在对象上等待的线程
     *          不释放对象锁
     */
}
